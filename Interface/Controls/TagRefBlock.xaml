<UserControl x:Class="InfiniteRuntimeTagViewer.Interface.Controls.TagRefBlock"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="25">
    <Grid x:Name="parent_element" Background="#606060" Margin="5,5,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="80*" MinWidth="80" />
            <ColumnDefinition Width="250*" MinWidth="250" />
            <ColumnDefinition Width="50" />
        </Grid.ColumnDefinitions>

        <ComboBox x:Name="taggroup" Grid.Column="1" SelectedIndex="0" BorderBrush="#fff" Margin="1">
            <!--ComboBoxItem Content="Null" IsSelected="True" /-->
        </ComboBox>

        <Button x:Name="tag_button" Grid.Column="2" Content="tag\wahtever\test\amongus\sussy" Margin="1" />
        <Button x:Name="goto_button" Grid.Column="4" Content="goto" Margin="1" />

        <TextBlock Grid.Column="0" x:Name="value_name" Foreground="#fff"
                   Text="name test" HorizontalAlignment="Stretch" Margin="5,0,5,0" />

    </Grid>
</UserControl>